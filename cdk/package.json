{
  "name": "cdk",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all",
    "format": "prettier --write 'lib/**/*.ts'",
    "lint": "eslint 'lib/**/*.ts' --fix",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.185.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.8.0"
  }
}
